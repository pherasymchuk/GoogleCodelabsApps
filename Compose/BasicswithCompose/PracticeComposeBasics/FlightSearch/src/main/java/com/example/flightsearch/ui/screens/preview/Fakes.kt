package com.example.flightsearch.ui.screens.preview

/*
object Fakes {
    fun fakeFlights(amount: Int): List<Flight> = List(amount) {
        Flight(
            id = it,
            departureAirport = Airport(
                id = 0,
                name = "Departure Airport 1",
                iataCode = "DAI1",
                passengers = Random.nextInt(30, 150)
            ),
            arrivalAirport = Airport(
                id = it,
                name = "Arrival Airport ${it + 1}",
                iataCode = "AAI${it + 1}",
                passengers = Random.nextInt(30, 150)
            ),
            isFavorite = Random.nextBoolean()
        )
    }
}
*/
